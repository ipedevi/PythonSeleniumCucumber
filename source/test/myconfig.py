# myconfig.py

# Selenium Configuration
SELENIUM_CONFIG = {
    "browser": "chrome",  # Select "chrome" or "firefox"
    "headless": False,    # True for headless mode, False for UI
    "initial_page": "https://www.google.es"
}